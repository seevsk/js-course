// DOM el temas importante que debes dominar - Document Object Model - es el documento HTML en si podiendo seleccionar los elementos y modificarlos


let elemento;

elemento = document.all; // todo el documento HTML   
elemento = document.head; // toda la cabecera
elemento = document.body; // todo el cuerpo
elemento = document.domain; // dominio actual
elemento = document.forms; // formularios
elemento = document.forms[0].id; // id
elemento = document.forms[0].method; // metodo 
elemento = document.forms[0].classList; // clases
elemento = document.forms[0].action // action

elemento = document.links; 
elemento = document.links[4]; 
elemento = document.links[4].classList; 
elemento = document.links[4].className;

elemento = document.images;
elemento = document.images[2];

elemento = document.scripts;






console.log(elemento);
