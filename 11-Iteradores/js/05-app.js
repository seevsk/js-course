// do While - se ejecuta al menos una vez y despues verifica la condicion al menos si se cumple o no (No tiene parentesis)

let i = 0; // Inicio

do {
    console.log(`Numero ${i}`);

    i++;  // Incremento
} while (i < 10); // Condicion

// Aunque no cumpla la condicion igual se ejecuta algo 